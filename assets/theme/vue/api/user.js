'use strict'

/**
 * Include dependecies.
 */

import axios from 'axios'

/**
 * Define user endpoint connections.
 */

const user = {

  /**
   * Login a user using the specified credentials.
   */

  loginUser () { },

  /**
   * Logout a user.
   */

  logoutUser () { },

  /**
   * Register a user using the specified credentials.
   */

  registerUser () { }
}

/**
 * Export endpoint connections.
 */

export { user }
